# GitHub personal access token (classic or fine-grained, read:contents scope)
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# Target repo to analyze (owner/repo format)
REPO=microsoft/playwright

# Path inside the repo where spec files live (leave empty to search entire repo)
SPEC_PATH=tests
